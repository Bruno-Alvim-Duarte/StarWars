import { createTheme } from '@mui/material';

const theme = createTheme({
  status: {
    danger: '#FFFF00',
  },
  palette: {
    secondary: {
      main: '#FFFF00',
    },
  },
});

export default theme;
